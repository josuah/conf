#!/bin/sh -eu

base=$(cd "${0%/*}/.."; pwd)

for path in /var/spool/adm/*/current; do
	case "$path" in (*"/*/"*) continue ;; esac

	host=${path%/current}
	host=${host##*/}

	echo
	echo "$host"

	adm-monitor-show "$path"
done
echo
