#!/bin/sh -eu

cmd=${1:?}
shift

case $cmd in
(service)
	service=${1:?}

	"$base/bin/service" status "$service" >/dev/null 2>&1 \
		&& echo "service/$service ok" \
		|| echo "service/$service fail"
	;;
(exec)
	exit 1
	;;
esac
