#!/bin/sh -eu

var_timeout=5

. "${0%/bin/*}/bin/lib/var"

case $command in
( monitor )
	. "$base/monit/$name" >/dev/null & pid=$!
	sleep "$var_timeout" && kill "$pid" 2>/dev/null &
	wait "$pid"
	;;
esac
