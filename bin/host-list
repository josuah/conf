#!/bin/sh -e

host=host/$(hostname -s)

while read item; do
	case $item in
	(module/*)
		echo " \\_ $item"
		;;
	(role/*)
		echo " * $item"
		while read module; do
			echo "  \\_ $module"
		done <$item
		;;
	esac
done <$host
