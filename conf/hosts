# deployed on {{NOW}}
%% me=$(hostname -s)

# global
127.0.0.1		ip4-localhost localhost {{me}}
::1			ip6-localhost localhost {{me}}
ff02::1			ip6-allnodes
ff02::2			ip6-allrouters

# servers
%% awk '
%%	$3 == "A" { printf "%s\t\t%s\n", $4, $1 }
%%	$3 == "AAAA" { printf "%s\t%s\n", $4, $1 }
%% ' conf/zone/z0.is.zone
%%! cat /etc/hosts.local 2>/dev/null
