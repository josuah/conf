# by {{file}} at {{date}}
%% me=$(hostname -s)

127.0.0.1		localhost {{me}}
::1			localhost {{me}}

%% awk '
%%	$3 == "A" { printf "%s\t\t%s\n", $4, $1 }
%%	$3 == "AAAA" { printf "%s\t%s\n", $4, $1 }
%% ' /etc/adm/conf/zone/z0.is.zone

%% [ -f /etc/hosts.local ] && cat /etc/hosts.local
