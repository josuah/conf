# by {{file}} at {{date}}

127.0.0.1			localhost
::1				localhost

%% awk '
%%	$3 == "A" { printf "%s\t\t\t%s\n", $4, $1 }
%%	$3 == "AAAA" { printf "%s\t%s\n", $4, $1 }
%% ' /etc/adm/zone/*.zone

%% [ -f /etc/hosts.local ] && cat /etc/hosts.local
