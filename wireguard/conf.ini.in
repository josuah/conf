# generated from adm-config repo at ${NOW}

[Interface]
PrivateKey = ${WIREGUARD_PRIVATE_KEY}
ListenPort = 51820

# Peers
#foreach WIREGUARD_PEERS

[Peer]
PublicKey = ${WIREGUARD_PUBLIC_KEY_%}
Endpoint = ${@}.z0.is
AllowedIPs = ${WIREGUARD_NETWORKS_%}
#end
